CREATE TABLE registroAula(

	professor VARCHAR(100),
	matricula VARCHAR(80),
	disciplina VARCHAR(80),
	turma VARCHAR(80),
	dataRegistro VARCHAR(30),
	conteudo VARCHAR(350),
	atividade VARCHAR(350),
	ID INT PRIMARY KEY AUTO_INCREMENT
);
